-
  var bodyClasses = [];
  var titleBase = 'TogoMedium';
  var titleChild = '';
  var TBD = '#TBD';
  var BLANK = '_blank';
  var pageID = '';
  var lev = 0;
  var pathToRoot = '/';
  var lev1ID = '';
  var stanzaIDs = [];

block vars

-
  title = titleChild ? titleChild + ' | ' + titleBase : titleBase;
  bodyClasses.push(lev1ID);
  pageID = lev1ID;
  // for(let i = 0; i < lev; i++){
  //   pathToRoot += '../';
  // }
  var pathToAssets = pathToRoot + 'assets/';
  var pathToJS = pathToAssets + 'js/';
  var pathToCSS = pathToAssets + 'css/';
  var pathToLibs = pathToAssets + 'libs/';
  var pathToSVG = pathToAssets + 'images/svg/';
  var pathToImgLG = pathToAssets + 'images/lg/';
  var pathToImgMD = pathToAssets + 'images/md/';
  var pathToImgSM = pathToAssets + 'images/sm/';
  var URL_STANZA = "https://dbcls.github.io/togomedium-stanza/";
  // var URL_STANZA = "https://yohak.github.io/togomedium-stanza/";
  // var URL_STANZA = "http://localhost:8080/";
  var URL_API = "http://togomedium.org/sparqlist/api/";
  // var URL_API = "http://growthmedium.org/sparqlist_dev/api/";

doctype html
html(lang="ja", xmlns:og="http://ogp.me/ns#")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="format-detection", content="telephone=no")
    meta(name="viewport", content="width=device-width,initial-scale=1.0,minimum-scale=1.0")
    meta(name="description", content=`${meta.description}`)
    meta(name="keywords", content="")
    link(href="https://fonts.googleapis.com/css2?family=Fira+Sans+Condensed:wght@300;400;600&display=swap", rel="stylesheet")
    each stanzaID in stanzaIDs
      script(src=`${URL_STANZA}${stanzaID}.js`, type="module", async)
    link(href=`${pathToLibs}html5reset.css`, rel="stylesheet", type="text/css")
    link(href=`${pathToCSS}main.css${hash}`, rel="stylesheet", type="text/css")
    link(href=`${pathToAssets}images/misc/favicon.png`, rel="icon")
    block links

    meta(property="og:title", content="")
    meta(property="og:description", content="")
    meta(property="og:type", content="")
    meta(property="og:image", content="")
    meta(property="og:image:width", content="1200")
    meta(property="og:image:height", content="630")
    meta(property="og:url", content="")
    title= title
  body(class=bodyClasses, id=pageID)
    .document-wrapper
      include ../_include/header
    main
      block main
    script(src=`${pathToJS}main.js`)
    block scripts
    include ../_include/dbcls.html
