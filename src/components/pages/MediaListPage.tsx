import { FC } from "react";
import { ListStanza } from "../stanzas/ListStanza.tsx";

export const MediaListPage: FC = () => {
  return (
    <div className={"grow"}>
      <ListStanza />
    </div>
  );
};
